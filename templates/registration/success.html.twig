{% extends 'base.html.twig' %}

{% block title %}{{ 'registration.title'|trans }}{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-6 text-center">
        <h1>{{ 'registration.success'|trans({'%email%': email}) }}</h1>
        <a href="{{ path('app_login') }}" class="btn btn-primary mt-3">{{ 'nav.login'|trans }}</a>
    </div>
</div>
{% endblock %}
